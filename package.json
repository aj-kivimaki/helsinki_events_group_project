{
  "scripts": {
    "start": "concurrently \"cd backend && npm install && npm run start-backend\" \"cd frontend && npm install && npm run start-frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
